# N04 Runtime Integration Ledger

Canonical endpoint: `/api/soul-mesh`.

The runtime must retain N04's tool/document capability surface. Mesh routing is a transport boundary, not a replacement for the nucleus's existing functions.

Live E2E connectivity requires deployment plus request -> handler -> correlated response evidence; source inspection alone is insufficient.
